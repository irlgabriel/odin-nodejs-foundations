extends layout

block content

  div.form-wrapper
    h3= title
    form(action='' method='POST')
      label(for='name') Name
      input(type='text' name='name' placeholder='Category name' value=category ? category.name : '' required)
      
      button(type='submit' style='margin-top: .5rem; margin-left: .5rem') Submit