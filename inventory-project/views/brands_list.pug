extends layout

block content
  a(href="/")
    p Go back
  h1 Brands
  
  ul.brands_list
    each brand in brands
      li
        a(href=`/brands/${brand._id}/items`)
          p #{brand.name}

  a(href="/brands/new")
    p Create new Category