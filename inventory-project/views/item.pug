extends layout

block content
  a(href="/items")
    p Go Back
  i= item.category.name
  h3= item.brand.name + ' ' + item.name

  p(style="margin-left: 20px;") #{item.description}
  strong= 'Price: ' + '$' + item.price
  p= 'In-Stock: ' + item.stock
  a(href=item.url + "/delete")
    p Delete Item
  a(href=item.url + "/edit")
    p Edit Item