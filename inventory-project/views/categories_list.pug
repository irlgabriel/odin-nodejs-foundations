extends layout

block content
  a(href="/")
    p Go Back
  h1 Categories
  
  ul.category_list
    each category in categories
      li
        a(href=category.url + '/items')
          p #{category.name}